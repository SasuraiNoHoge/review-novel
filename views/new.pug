extends layout

block content
  form(method="post", action="/novels")
    div
      h5 作品名
      input(type="text" name="novelTitle")
    div
      h5 本文
      textarea(name="novelText")
    div
      h5 作品がより良くなるためにヒント (改行して複数入力可)
      textarea(name="questions")
    button(type="submit") 小説を投稿する